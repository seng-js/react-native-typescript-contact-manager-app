import * as React from "react";
import Layout from "../components/Layout/Layout";

const HomeScreen = () => {
     return (
        <Layout name="Home" title={''}/>
    );
}

export default HomeScreen;
